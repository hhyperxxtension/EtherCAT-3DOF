# TODO: разделение кулоновского и вязкого трения в dyncal

**Статус:** не сделано. Делать на месте, у робота (нужна повторная динамическая калибровка).
**Приоритет:** средний — текущая компенсация работает, но динамическая модель плеча слабовата.

---

## Проблема

Динамическая калибровка (`dyncal`) не разделяет кулоновское и вязкое трение.
В `dynamics_calib.json` кулоновский коэффициент плеча вышел ≈ **1.35 ‰**, тогда как
двунаправленный пробник (`gravcal`/`stiffscale`) честно меряет статическое трение
плеча ≈ **105 ‰**. Почти всё трение «утекло» в вязкий член. Итоговый остаток фита:

| ось        | RMS остатка | Кулон (calib) | Кулон (пробник) |
|------------|-------------|---------------|-----------------|
| плечо (0)  | 64.3 ‰      | 1.35 ‰        | ~105 ‰          |
| предплечье | 20.6 ‰      | −35.9 ‰       | ~54 ‰           |

### Почему так

Модель трения: `τ(q̇) = τ_c·sgn(q̇) + ν·q̇`. Для одного направления это прямая
`τ = τ_c + ν·q̇`, где **Кулон τ_c — отрезок на оси при q̇=0**, **вязкость ν — наклон**.

Две причины плохой обусловленности:

1. **Узкая скоростная сетка вдали от нуля.** Возбуждающие проезды идут на
   v = 0.2 / 0.3 / 0.4 рад/с — точки скучены, разброс скоростей S_q̇q̇ мизерный.
   Дисперсия оценки отрезка σ²(τ̂_c) = σ²·(1/n + q̇̄²/S_q̇q̇): множитель q̇̄²/S_q̇q̇
   огромен (среднее 0.3 далеко от нуля, разброс крошечный) → Кулон по сути
   экстраполируется назад к q̇=0 и определяется крайне ненадёжно.

2. **Коллинеарность столбцов sgn(q̇) и q̇.** При скоростях одного знака внутри
   проезда |q̇| почти постоянна, поэтому sgn(q̇) ≈ const и почти параллелен q̇.
   МНК произвольно «размазывает» вклад между ними (корреляция оценок ≈ 0.95).

Это не баг и не физика (физически Кулон от скорости не зависит) — это плохая
обусловленность задачи наименьших квадратов.

Наглядная демонстрация (интерактив): `D:\Diploma 2026\Калибровочные_json\friction_identifiability.html`.

---

## Решение (два приёма вместе)

### 1. Зафиксировать Кулон из двунаправленного пробника
Пробник на малой стабильной скорости даёт `friction=(fwd−rev)/2` — почти чистый
кинетический Кулон (вязкий вклад ν·q̇ на малой скорости пренебрежим). Берём его
как **известную константу**, а не как искомый параметр. Это убирает из регрессии
самый плохой столбец `sgn(q̇)`.

- Пробник делать НЕ на грани страгивания (эффект Штрибека завысит τ_c), а на
  стабильной малой скорости, где движение уже плавное, но ν·q̇ ещё ничтожен.

### 2. Расширить скоростную сетку в dyncal
Нужно для **вязкости**: чтобы поймать наклон ν, скорость должна заметно меняться.
Раздвинуть с 0.2–0.4 до ~0.1 … 1.5 рад/с, 5–6 уровней → S_q̇q̇ растёт на порядок,
ν определяется уверенно.

Приёмы лечат разные болезни: фиксация Кулона убирает коллинеарность,
широкая сетка убирает малый разброс скоростей.

---

## Где править (точечно)

### `src/dynamics.py`
- `dyn_basis()` (стр. ~49): убрать столбец `_sgn(qd0)` из базиса плеча (стр. 59) и
  `_sgn(qd1)` из базиса предплечья (стр. 64). Обновить `_NDYN = (5, 4)` → `(4, 3)`.
- `dyn_torque()`/`predict()`: добавлять зафиксированный Кулон вручную:
  `τ_friction_coulomb = TAU_C[j] * sgn(qd[j])`, где `TAU_C` берётся из пробника
  (сохранять в gravity_calib или отдельный friction_calib.json).
- `fit()` (стр. 102): перед фитом вычитать из `tau` не только G(q), но и
  зафиксированный Кулон `TAU_C[j]·sgn(q̇_j)`. Регрессировать только
  инерцию + Кориолис + вязкость.

### `src/config.py`
- `DYNCAL_VMAX` (стр. 187): 0.6 → ~1.5.
- `DYNCAL_ACCELS` (стр. 188): оставить разнообразие, можно расширить.
- Сетка `vas` в `controller.dyncal` (стр. 604–606): задать 5–6 уровней
  скорости в диапазоне ~0.1 … 1.5, например
  `[(0.1,0.5),(0.3,1.0),(0.6,1.5),(0.9,2.0),(1.2,2.0),(1.5,2.0)]`.
  ВНИМАНИЕ: проверить, что на высокой v траектории не упираются в Z_FLOOR и
  лимиты INT32 (в dyncal уже есть guard `_traj_floor_ok` и `counts_in_int32`).

### Источник зафиксированного Кулона
- `controller.dyncal` (стр. 589): подгружать τ_c из результатов пробника
  (`gravity_samples` хранит per-pose `friction`; усреднить |friction| по позам:
  плечо ≈ 105 ‰, предплечье ≈ 54 ‰) и передавать в `DynamicsModel`.

---

## Проверка после правок
1. Перекалибровать: `gravcal` → (пробник даёт Кулон) → `dyncal` с широкой сеткой.
2. Ожидаемо: RMS остатка плеча упадёт заметно ниже 64 ‰; коэффициент вязкости
   станет физичным (без «поглощённого» Кулона).
3. Прогнать на холостую (без груза) и сверить, что наблюдатель нагрузки даёт
   около нуля payload-остаток в статике и на разных скоростях.
